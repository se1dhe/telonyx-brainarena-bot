server:
  port: ${APP_PORT:8080}

spring:
  application:
    name: brainarena-api

brainarena:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:*}

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
